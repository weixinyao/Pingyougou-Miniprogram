<!--pages/home/homecomponent/w-recommend/w-recommend.wxml-->
<view class="recommend">
  <block wx:for="{{recommends}}" wx:key="{{index}}">
  <view class="recommend-item">
    <image src="{{item.image}}"></image>
    <text>{{item.title}}</text>
  </view>
  </block>
</view>
